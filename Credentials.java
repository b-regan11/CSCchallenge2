public class Credentials {
    private static final String username = ""; // insert GitHub username within the ""
    private static final String token = ""; // insert GitHub token within the ""

    // Getter for username
    public static String getUsername() {
        return username;
    }

    // Getter for token
    public static String getToken() {
        return token;
    }
}